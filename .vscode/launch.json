{
    "configurations": [
        {
            "name": "Ejecutar nodejs",
            "program": "${file}",
            "request": "launch",
            "type": "node",
            //-- Arrancar desde el directorio donde esta el ejemplo
            "cwd": "${workspaceFolder}/${relativeFileDirname}"
        }
    ]
}